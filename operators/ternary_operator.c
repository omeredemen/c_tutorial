#include <stdio.h>

int main(){

    int a = 5;
    int b = 6;

    if (a == b ? 1 : 0)
    {
        printf("a and b is equal\n");
    } else {
        printf("a and b is not equal\n");
    }
    
    return 0;
}